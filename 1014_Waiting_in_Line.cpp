#include<iostream>
#include<string>
#include<sstream>
#include<queue>
#include<vector>
using namespace std;

int main(){
    int N,M,K,Q;
    string line;
    istringstream s;
    getline(cin,line);
    s.str(line);
    s>>N>>M>>K>>Q;
    vector<queue<int>>win(N);
    getline(cin,line);
    s.clear();
    s.str(line);
    int spent;
    while(s>>spent){
        for()
    }
}